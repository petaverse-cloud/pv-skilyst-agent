{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: the shell drives the local runtime over loopback HTTP with a per-process token, so it needs no filesystem or shell permissions of its own.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
